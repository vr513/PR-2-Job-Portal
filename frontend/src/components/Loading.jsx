import { Spinner } from "@chakra-ui/react";
import React from "react";

const Loading = () => {
    return (
        <>
            <Spinner size={'5rem'} />
        </>
    )
}

export default Loading;